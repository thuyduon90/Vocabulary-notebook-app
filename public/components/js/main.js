$(document).ready(function () {
    // var bodyWith = ($('body').width());
    // var controlWidth = $('.control').width();
    // var searchWidth = 0.5*controlWidth;

    // $('.search').width(searchWidth);
    // console.log("Search: "+$('.search').width());
    // $(window).resize(function () { 
    //     $('.search').width(searchWidth);
    //     console.log("Search: "+$('.search').width());
    // });
});